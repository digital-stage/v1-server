interface ThreeDimensionProperties {
  // Position relative to stage
  x: number;
  y: number;
  z: number;
  // Rotation relative to stage
  rX: number;
  rY: number;
  rZ: number;
}
export default ThreeDimensionProperties;
